{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$file\" in *src/*.ts|*src/*.tsx) pnpm exec tsc --noEmit 2>&1 | head -50 ;; esac; exit 0",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
